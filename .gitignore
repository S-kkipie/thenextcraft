node_modules
.next
.turbo
out
dist
*.log

# env
.env
.env.local
.env*.local

# convex: _generated IS committed (one Convex owner runs `convex dev`; the team
# shares the typed api via git so nobody else needs a Convex login to build).
.convex

# convex ai-files skill payloads (reinstall: npx convex ai-files install)
.agents/
packages/backend/.claude/

.DS_Store

# git worktrees created under .claude
.claude/worktrees/

# skills de agentes instaladas localmente (.agents/ ya está arriba)
.claude/
skills-lock.json

# referencias de diseño (local)
/ui/

# docs de diseno internos (local)
docs/gamification.md
